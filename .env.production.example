# Run with: docker compose --env-file .env.production -f docker/compose.prod.yml up -d
DSH_PORT=3080
DEEPSEEK_API_KEY=

# Alpha supports one process-scoped tenant/user identity per deployment.
FUTURESTAFF_TENANT_ID=
FUTURESTAFF_USER_ID=

SELECTION_CENTER_BASE_URL=https://selection-center.example.com/
SELECTION_CENTER_API_KEY=
SELECTION_CENTER_TIMEOUT_MS=10000
