server {
    listen 443 ssl http2;
    server_name futurestaff.example.com;

    ssl_certificate /etc/letsencrypt/live/futurestaff.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/futurestaff.example.com/privkey.pem;

    # Add the organization's authenticated gateway, VPN, or IP allowlist here.
    # Do not expose Alpha publicly without an access-control boundary.

    location / {
        proxy_pass http://127.0.0.1:3080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_buffering off;
        proxy_read_timeout 3600s;
    }
}
