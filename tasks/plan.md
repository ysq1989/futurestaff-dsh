# M4 Vietnam visa plan

1. Approve the multi-management-tenant and collector specification.
2. Enumerate the existing development collector MCP and freeze its read/write capability matrix.
3. Add collector visa approval tests to `fs-core`, then implement fail-closed enforcement.
4. Mount the collector MCP in `futurestaff-alpha` with server-only credentials and deployment tests.
5. Replace the placeholder visa skill with collector-safe operating instructions.
6. Execute one test flow: list products, create an approved draft, query progress and verify application-count reporting.
7. Review, run full checks, commit, push and deploy.

The visual workbenches share the FutureStaff visa backend but are developed in the platform application repository. This repository delivers the DSH/MCP slice first.
